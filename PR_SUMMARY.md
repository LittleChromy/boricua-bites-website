# Pull request summary

This pull request updates the Boricua Bites LLC website with the following changes:

- Adds storefront hero image and site logo (logo background removed) to /assets/images
- Uses the storefront photo as the hero background with a dark overlay for readability
- Displays the provided logo next to the site title in the header
- Updates business hours throughout the site to the requested schedule
- Replaces the menu with the full menu items, descriptions, and prices (sourced from the DoorDash listing provided by the owner), organized into categories
- Adds simple tab UI for the menu categories and updates the order form select options

Please review the branch 'BoricuaBitesLLC' for the full diff and verify the images render as expected.
